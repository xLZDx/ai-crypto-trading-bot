# Virtual Environment
venv/
env/

# Sensitive Information (API Keys)
.env

# Python Cache
__pycache__/
*.pyc

# Data & Logs (Dynamically generated)
data/raw/
data/state.json
data/trades.json
data/control.json
logs/

# Models (Large binary files)
models/*.joblib
models/*.pt
models/*.ckpt
models/archive/

# Runtime-only data files (never commit)
data/orchestrator_state.json
data/train_all_models.lock
data/training_current.json
data/*.tmp
tft_train.bat

# Generated parquet / DuckDB stores (runtime data, multi-GB)
data/parquet/
data/db/
data/cache/
questdb/
*.duckdb

# Build / training artefacts
build/
darts_logs/

# Stray editor / pip artefacts
=*
* - Copy.md
.claude/scheduled_tasks.lock