ccxt==4.2.83
python-dotenv==1.0.1
requests==2.31.0
filelock>=3.12.0
defusedxml>=0.7.1

# Web / API
flask
websockets
mcp
debugpy
psutil

# Data & ML
pandas
scikit-learn
imbalanced-learn>=0.13   # SMOTE for scalping label rebalance (v3.1 step 6)
joblib
numpy

# NLP / Sentiment
vaderSentiment
feedparser
beautifulsoup4
youtube-transcript-api
google-genai

# Quantitative / Deep Learning (Phase 1-3)
darts
neuralforecast
arch
statsmodels
vectorbt
yfinance
duckdb>=0.10.0

# Time-series database
# QuestDB Python client retired — see commits 43db156…b64b733 (DB migration to DuckDB+Parquet).
# duckdb / pyarrow already pulled in by other entries.

# Phase 0 — Institutional upgrade: data store + transport
pyarrow>=15.0.0       # Parquet I/O for partitioned tick store
pyzmq>=25.0.0         # ZeroMQ data-plane (PUB/SUB orderflow, PUSH/PULL training batches)
fastapi>=0.110.0      # Control plane for parquet/training/cluster ops on port 8100
uvicorn[standard]>=0.27.0
msgpack>=1.0.7        # Compact wire format for ZeroMQ payloads

# Phase 1 — Microstructure data layer
pykalman>=0.10.1      # Kalman filter for price noise cleaning (arch plan §3)
# Binance L2 order book uses native `websockets` (already listed above) — no ccxt.pro needed.

# Phase 4 — Portfolio optimization
cvxpy>=1.4.0          # CVaR portfolio optimizer (arch plan §13)
